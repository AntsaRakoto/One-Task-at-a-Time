<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:padding="0dp">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/top_toolbar"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:background="@drawable/topbar_bg"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        app:contentInsetStartWithNavigation="0dp"
        app:contentInsetLeft="0dp"
        app:contentInsetRight="0dp"
        app:layout_constraintTop_toTopOf="parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/backBtn"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_centerVertical="true"
                android:clickable="true"
                android:focusable="true"
                android:src="@drawable/ic_arrow_back" />

            <ImageView
                android:id="@+id/profileBtn"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_profile"
                android:clickable="true"
                android:focusable="true"/>

        </RelativeLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- Gros titre (Brrr... une journée fraîche s'annonce !) -->
    <TextView
        android:id="@+id/title_main"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Brrr... une\njournée fraîche\ns’annonce !"
        android:textAlignment="center"
        android:gravity="center"
        android:textSize="34sp"
        android:lineSpacingExtra="2dp"
        android:textColor="@color/text_main"
        android:fontFamily="@font/merriweather_regular"
        app:layout_constraintTop_toBottomOf="@id/top_toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <!-- Image centrale (thermometer) -->
    <ImageView
        android:id="@+id/image_thermometer"
        android:layout_width="160dp"
        android:layout_height="220dp"
        android:src="@drawable/weather_froid"
        android:contentDescription="Thermomètre"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toBottomOf="@id/title_main"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <TextView
        android:id="@+id/subtext"
        android:layout_width="327dp"
        android:layout_height="114dp"
        android:layout_marginTop="0dp"
        android:fontFamily="@font/merriweather_regular"
        android:gravity="center"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:text="Un bon moment pour te concentrer sur tes objectifs et organiser ton espace."
        android:textAlignment="center"
        android:textColor="@color/text_main"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/image_thermometer" />

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/subtext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/btn_projet"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginTop="20dp"
        android:backgroundTint="@color/beige_fonce"
        android:textColor="@color/text_main"
        android:textSize="20sp"
        android:textAlignment="center"
        android:textAllCaps="false"
        android:fontFamily="@font/merriweather_regular"
        android:text="Voir projet actuel"
        android:gravity="center"
        android:padding="16dp"
        app:cornerRadius="24dp"
        app:elevation="4dp"
        app:layout_constraintTop_toBottomOf="@id/subtext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
